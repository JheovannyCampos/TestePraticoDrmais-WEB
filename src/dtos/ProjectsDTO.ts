//Esta interface representa a tipagem dos atributos de cada projeto

export interface ProjectsDTO {
        id: string;
        idprojeto: string,
        ds_titulo: string,
        ds_descricao: string,
}